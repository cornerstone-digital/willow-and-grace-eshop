import Server from './main/MainServer'

async function bootstrap() {
  try {
    // $log.debug('Start server...')
    // const platform = await PlatformExpress.bootstrap(Server, {
    //   // extra settings
    // })
    // await platform.listen()
    // $log.debug('Server initialized')
  } catch (er) {}
}

bootstrap()
